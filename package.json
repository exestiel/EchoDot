{
  "name": "echodot",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "powershell -ExecutionPolicy Bypass -File ./scripts/build-audio-zip.ps1 && node ./scripts/verify-media-paths.mjs && tsc && vite build",
    "preview": "vite preview",
    "check:pages-media": "node ./scripts/check-pages-media.mjs"
  },
  "devDependencies": {
    "@types/dompurify": "^3.0.5",
    "typescript": "~6.0.2",
    "vite": "^8.0.4"
  },
  "dependencies": {
    "isomorphic-dompurify": "^3.8.0",
    "marked": "^18.0.0"
  }
}
